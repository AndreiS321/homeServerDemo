---
- name: Ping playbook
  hosts: all
  tasks:
    - name: Ping hosts
      ansible.builtin.ping:

    - name: Print
      ansible.builtin.debug:
        msg: "{{ ansible_facts }}"

    - name: Print
      ansible.builtin.debug:
        msg: "{{ ansible_facts['system'] }}"
